The markdown files in the Docs directory are the source of truth for the RNF system design.

Use the markdown documentation to determine:
- system architecture
- gameplay mechanics
- feature structure
- services and systems

The images in Docs/ui_mockups are older exploratory design references and should NOT override the markdown documentation.

If there is any conflict between UI mockups and markdown files, follow the markdown files.
